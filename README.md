# CuteColors

GitHub Pages mirror for the Tilda site. The workflow fetches the live pages and publishes them to Pages on every push to `main`.

Asset URLs now resolve relative to the project Pages path so images, CSS, and JS load correctly under `/cutecolor/`.
